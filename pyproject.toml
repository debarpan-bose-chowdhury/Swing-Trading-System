[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "swing-trading-system"
version = "0.1.0"
description = "Swing trading system service"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
include = ["app*"]
